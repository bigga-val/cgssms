{% extends 'base.html.twig' %}

{% block title %}New Contact{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">

                <div class="card">
                    <div class="card-header">
                        <h3>Creer un nouveau contact</h3>
                    </div>
                    <div class="card-body">
                        {{ include('contact/_form.html.twig') }}

                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_contact_index') }}">Voir la liste</a>

                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
