<form method="post" action="{{ path('app_templatesms_delete', {'id': templatesm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ templatesm.id) }}">
    <button class="btn">Delete</button>
</form>
