{{ form_start(form) }}
{#    {{ form_widget(form) }}#}
<div class="form-group">
    <label for="">Titre *</label>
    {{ form_widget(form.titre, {'attr': {'class':'form-control', 'placeholder':'Titre du message'}}) }}
</div>
<div class="form-group">
    <label for="">Texte * (Max: 160 caracteres)</label>
    {{ form_widget(form.texte, {'attr': {'class':'form-control', 'placeholder':'Corps du message', 'maxlength':160}}) }}
</div>
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
