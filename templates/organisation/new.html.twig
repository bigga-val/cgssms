{% extends 'base.html.twig' %}

{% block title %}New Organisation{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header bg-black">
                        <h3>Creer une nouvelle organisation</h3>
                    </div>
                    <div class="card-body">
                        {{ include('organisation/_form.html.twig') }}

                    </div>
                    <div class="card-footer">
                        <a href="{{ path('app_organisation_index') }}">Voir la liste</a>

                    </div>
                </div>

            </div>
        </div>
    </div>
{% endblock %}
